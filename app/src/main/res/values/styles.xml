<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextViewList">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/default_half_padding</item>
        <item name="android:layout_marginLeft">@dimen/default_half_padding</item>
        <item name="android:layout_marginRight">@dimen/default_half_padding</item>
    </style>

    <style name="TextViewList.Title">
        <item name="android:textSize">@dimen/title_text_size</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="TextViewList.Description">
        <item name="android:textColor">@color/dull_grey</item>
    </style>

</resources>
